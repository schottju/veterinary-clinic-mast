= form_tag user_animals_path, method: :get do
  = hidden_field_tag :direction, params[:direction]
  = hidden_field_tag :sort, params[:sort]
  .form-inline
    = text_field_tag :search, params[:search], placeholder: 'identification ou nom animal', class: 'form-control', size: 45
    = submit_tag "Recherche", name: nil, class: 'btn btn-default'
    - unless current_user.pacjent?
      .pull-right
        = link_to 'Nouvel animal', new_user_animal_path(params[:user_id]), class: "btn btn-default"
