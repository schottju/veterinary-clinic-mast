= form_tag user_medical_records_path(user), method: :get do
  = hidden_field_tag :direction, params[:direction]
  = hidden_field_tag :sort, params[:sort]
  .form-inline
    = text_field_tag :search, params[:search], placeholder: 'Entrez le numéro ou la date de la visite', class: 'form-control', size: 32
    = submit_tag "Recherche", name: nil, class: 'btn btn-default'
    - unless current_user.pacjent?
      .pull-right
        = link_to 'Nouveau fichier', new_user_medical_record_path(user), class: "btn btn-default"
